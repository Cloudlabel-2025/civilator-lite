{
  "buildCommand": "cd WebApp/frontend && npm install && npm run build",
  "outputDirectory": "WebApp/frontend/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}